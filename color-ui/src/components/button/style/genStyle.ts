import { ButtonPropsType } from "../src/button";

/**
 * @description 根据按钮的类型 <type> 生成指定的 class 集合
 */
export const genButtonTypeClass = (props: ButtonPropsType) => {
    return {

    };
};

/**
 * @description 根据按钮的尺寸 <size> 生成指定的 class 集合
 */
export const genButtonSizeClass = (props: ButtonPropsType) => {
    return {

    };
};